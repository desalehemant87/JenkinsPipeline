# To initialize the git reposiory
git init

# To add the files to the git repo
git add . (This will add all files , specific file name also can be provided)

#To check the status of git files
git status

# Provide the email for the repository
git config --global user.email "desalehemant87@gmail.com"

# How to commit codes to repository
git commit -m "first commit"

# Provide the remote origin of repository
git remote add origin https://github.com/desalehemant87/JenkinsPipeline.git

# Push the code to specific branch of repository
git push -u origin master
